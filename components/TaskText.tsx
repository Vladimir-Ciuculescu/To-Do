import { Text } from 'native-base'
import React from 'react'
import Animated, {
  interpolateColor,
  useAnimatedStyle,
  useSharedValue,
} from 'react-native-reanimated'

interface TaskTextInterface {
  label: string
}

const TaskText: React.FC<TaskTextInterface> = ({ label }) => {
  return <></>
}
